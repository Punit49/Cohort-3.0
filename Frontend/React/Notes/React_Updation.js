// Virtual DOM is a lightweight copy of the actual DOM, react used to update only the changed part of the website. 
// It is stored in js memory not inside browser. 

// React Fiber is React's internal reconciliation engine introduced to make rendering faster, interruptible, and more efficient. Instead of processing the entire component tree in one blocking operation, Fiber breaks rendering work into small units called fibers. This allows React to pause, resume, prioritize, or even discard work based on the importance of updates, keeping the UI responsive. Each fiber represents a component and stores the information React needs to update it efficiently.

