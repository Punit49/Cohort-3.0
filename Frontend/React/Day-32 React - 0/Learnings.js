// ? Topics Covered - 
// * What is React js
// * History of react
// * Why it was created
// * Real vs Virtual DOM
// * React.createElement
// * ReactDOM, render()

// ? Learnings - 
// React is js library for developing complex frontend ui, in a single page application.

// It was developed by facebook in 2011, and officially launched in 2013, jordan walkie was an fb engineer, who introduced the concept of virtual DOM, which intialliy called as fax.js. 

// React.createElement takes 3 arguments - element, props (attributes, arguments), children (text, other element).

// In react if we create a children of a parent, then its child also lies under props. 

// Our Main DOM cant understand virtual dom so it gives us [object Object]
// ReactDOM acts as a middleware for solving this problem, it takes one elemenet (root) from real DOM, and render all react elements into that element. 
